Int.create(x=0)

Loop.while(x.lessThan(5); {

    Int.set(x=x.add(1))

    Logic.if(x.equals(3); {
        Terminal.writeLine("x is"; x; "Now continuing from loop")
        Loop.continue()
    })
    Terminal.writeLine("INSIDE: x is"; x)

})

Terminal.writeLine("OUTSIDE: x is"; x)
