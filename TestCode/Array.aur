Array.create(myArray=Array.from(1; 2; 3; type=Int))
Terminal.writeLine("myArray is"; myArray)

Terminal.writeLine("myArray has a length of"; myArray.length)
Terminal.writeLine("Element at 0 is"; myArray.at(0))
Terminal.writeLine("Element at 2 is"; myArray.at(2))
